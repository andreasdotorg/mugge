"""D-020 Monitoring Web UI — Pi Audio Workstation dashboard.

Unified SPA with four views: Monitor, Measure, System, MIDI.
Stage 1 implements Monitor and System; Measure and MIDI are stubs.

Run from the web-ui directory:
    uvicorn app.main:app --host 0.0.0.0 --port 8080
"""
